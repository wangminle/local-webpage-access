## 背景与范围判定

CHK-208 是对原审阅报告的**分级/措辞复核**，不涉及未修复代码（14 条 bug 上一轮已逐条核实：12 条修复、2 条 Windows 原生按「关闭」处理）。用户本次「修复所有问题」= 让报告（及相关记录）与 CHK-208 校正一致。**本次不改任何业务代码、不加测试**（4 处测试缺口 + BUG-508 符号链接残留为独立工程项，已在上一轮列出，不在 CHK-208 范围，留给后续）。

唯一改动文件：
- `design/plans/全量代码审阅报告-20260814.md`（报告校正）
- `task-list.md`（新增 CHK-212 文档更新条目，经 CLI 校验）

---

## 报告校正（7 处，严格对照 CHK-208）

### 1. 顶部结论行（第 6 行）
改「确认 major 级 14 条」→「发现 14 个候选重要问题，其中约 8～10 个达到 major，分级经 CHK-208 复核调整（2 条 Windows 原生移出产品队列、数条降级或收窄影响）」。删去把 Windows 失败混入产品结论的措辞。

### 2. 新增「复核校正（CHK-208）」提示块（标题行下）
插入一段引用块，说明：本报告为初版审阅产物，分级/基线措辞已由 CHK-208 校正（Windows 两条依据 README.md:32「Windows 原生 hard fail」移出产品队列）；并注明 14 条后续处置：12 条已修复（BUG-508~510/512/514~521）、2 条已关闭（BUG-511/513）。链接 CHK-208 / CHK-211。

### 3. §一 基线表（第 14-15 行）
mypy「9 处报错」与 pytest「退出码 1」明确标注为 **Windows 原生环境快照**，补一行「支持平台基线（macOS，CHK-208/CHK-211 复测）」：ruff ✅ / compileall ✅ / mypy 0 issues（74 文件）/ pytest 退出码 0（2033 passed、10 项 Docker 集成按配置跳过）。

### 4. §二 Major 表（第 19-34 行）—— 加「分级」列 + 逐条收窄
新增「分级」列，按 CHK-208 标注，并校正被夸大的「影响」文字：
- #1/BUG-508：影响改为「越界路径可进入分类结果，但导入安全解压无法主动创建 current/ 外文件，需预先存在的外部目录才能形成暴露」→ 分级 **P2/边界加固**
- #2/BUG-509：分级 **P1**
- #3/BUG-510：**P0**（不变）
- #4/BUG-511：影响标注「Windows 原生非支持平台（README.md:32 hard fail），不影响受支持运行环境」→ **移出产品缺陷队列**
- #5/BUG-512：删去「疑似」，改为「已由 procps 官方手册确认（`-l` 仅 comm 截断 15 字符，`-a` 才含完整命令行），待 Linux 实机回归」→ **P1**
- #6/BUG-513：同 #4 标注 → **移出产品缺陷队列**
- #7/BUG-514：**P0**（不变）
- #8/BUG-515：**P0**（不变）
- #9/BUG-516：影响收窄为「切到 builtin 时，损坏 manifest 对应的**运行中**静态实例无进程承接可能下线（非所有 manifest 加载失败场景）」→ **P0（可用性）**
- #10/BUG-517：影响改为「主要发生于 `gateway on` 处理残留 builtin 的过渡/恢复场景；正常 gateway switch 已有外层回滚兜底」→ 分级 **P0→P1**
- #11/BUG-518：**P0**（不变）
- #12/BUG-519：**P0**（不变）
- #13/BUG-520：**P0**（不变）
- #14/BUG-521：影响改为「src 内无生产调用方，属潜伏缺陷，接线前应修」→ 分级 **major→minor/P2**

### 5. §三 Minor（第 38 行）
「minor 级约 90 条」→「约 90 个候选疑点（尚未逐条独立确认，原始分组报告中混有不确定项、设计权衡与已撤销项）」。

### 6. §四 优先级（第 50-66 行）—— 按 CHK-208 重写
- **P0（数据完整性/状态机/可用性）**：#3/510、#7/514、#8/515、#11/518、#12/519、#13/520；#9/516（可用性标准）
- **P1（平台可靠性/正确性）**：#2/509、#5/512、#10/517
- **P2/边界加固**：#1/508、#14/521
- **移出产品缺陷队列（非支持平台）**：#4/511、#6/513

### 7. §五 测试套件现状（第 68-70 行）
开头明确「以下为 Windows 原生环境快照，非仓库当前基线」；补一句支持平台结论：macOS 上全量 pytest 退出码 0、10 项 Docker 集成按配置跳过（CHK-208/CHK-211）。将「除 lifecycle 两项外均为环境性」措辞降格为「Windows 环境归因，未在全部支持平台逐一验证，不作确定结论」。

---

## task-list（1 处追加）
按 AGENTS.md「文档更新」记录本次报告校正：新增 **CHK-212**（检查事项 / 已完成 / 报告按 CHK-208 校正分级与措辞，登记支持平台基线），用 `task_list_cli.py add` 写入并 `check` 校验。**不改动** BUG-508~521 已有条目（CHK-208 已决定保留其原状态）。

---

## 完成判定
- 报告 7 处校正全部落地，无残留「确认 major 14 条」式绝对结论；
- task-list 通过 CLI `check`；
- 不触碰任何业务代码/测试，故无需重跑测试（但报告内会如实登记上一轮已绿的基线数字）。

## 不做（明确边界）
- 不写 4 处缺失回归测试、不修 BUG-508 符号链接残留（均非 CHK-208 范围，上一轮已列为可选后续）；
- 不改 BUG-508~521 历史 task 条目；
- 不提交 git（除非你额外要求）。