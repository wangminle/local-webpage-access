# ---- Python -----------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so

# 打包 / 安装产物（hatchling / pip）
dist/
build/
*.egg-info/
.eggs/
*.egg
pip-wheel-metadata/

# 源码发布 zip（scripts/pack-release-zip.sh 无参执行时的默认输出在仓库根，BUG-202）
/归档.zip

# 虚拟环境
.venv/
venv/
env/

# ---- 测试与质量工具 ---------------------------------------------------------
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/
.mypy_cache/
.ruff_cache/
.hypothesis/
tmp-test/

# ---- 编辑器与 IDE -----------------------------------------------------------
# 注意：目录整目录排除（.vscode/）会让下面的 ! 反向包含失效（git 不能重新包含
# 父目录已被排除的文件），必须用 /* 通配内容再排除 extensions.json
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
*.swo
*~

# ---- 操作系统 ---------------------------------------------------------------
.DS_Store
Thumbs.db
desktop.ini

# ---- 本地 lwa 工作区（init/import 生成；可在仓库根或 runtime/ 下，勿提交）---
/runtime/
/local-web.yml
/inbox/
/apps/
/registry/
/run/
/logs/
/static-gateway/
/manager/
/skills/
/templates/

# SQLite 运行时文件
*.db-shm
*.db-wal

# ---- 敏感配置 ---------------------------------------------------------------
.env
.env.*
!.env.example
*.local.yml

# ---- 本地运维与问题收集（勿提交）-------------------------------------------
/问题收集/

# ---- AI 工具本地配置（不同步到 GitHub）--------------------------------------
CLAUDE.md
.claude/
.codex/
.zcode/
AGENTS.md

# 代码审查报告（本地生成）
_review_reports/

# ---- design 本地目录（issues/reviews 不同步到 GitHub）------------------------
design/4-issues/
design/5-reviews/
