# V1 发布清单（WBS-30.11）

本清单用于 V1 正式发布前的最终核对。逐项确认后方可打 tag 发布。

## 代码与版本

- [ ] `pyproject.toml` 的 `version` 已设为目标发布版本（如 `0.8.0` / `1.0.0`）；该值在 pip 安装时固化为包元数据，是 git 不可用时的版本来源。
- [ ] 发布提交主题为 `V<x.y.z>-Build<NNNN>-<YYYYMMDD>` 格式：`resolve_version()`（version_info.py）在 git 检出内优先解析最新 commit 主题，其次包元数据，最后 `_FALLBACK_VERSION`；主题不带 `V` 版本前缀时 `lwa --version` 与管理页将显示旧版本（DOC-120）。
- [ ] `src/local_webpage_access/cli/` 包入口（`cli/__init__.py` 的 `version` 命令 / `version_info.py`）可正常解析版本；`python3 -m local_webpage_access` 与 `python3 -m local_webpage_access.cli` 均可调用。
- [ ] `README.md` 的特性、命令、路线图与实际实现一致（Phase 0~7 全部「已完成」；含浏览量 / 冗余 / 运维手册 / 自启动 `lwa autostart`（含 `doctor-hints`）/ 宿主机装配 `setup|init --default|--full|--resume` / `doctor --profile full` / `lwa capabilities` / Full Profile 能力闭环与周期刷新 / `lwa update` 默认重启 gateway / IMP-023 别名资源错位检测 / IMP-035 安全删除 / IMP-036 正式平台矩阵 / IMP-037 `gateway switch` / IMP-038·040 访问复核与 LAN 新鲜度 / IMP-039 `cancel-build` / IMP-041 remove 阶段日志 / IMP-042 `lwa workspace relocate` / IMP-043 显示名（含产物目录 title） / `lwa recover` / `lwa pageviews` / V0.6.12 裸 mv 防复发 / V0.6.13 挂载 fail-safe、pageviews 轮转与显示名回填加固 / **V0.7.0 IMP-046 Token 168h 自动轮换** / **IMP-047 本机文件夹源导入与更新** / **V0.7.1 IMP-051 选择文件夹（仅 loopback）与导入/`lwa update` 护栏** / **V0.7.2 IMP-014 容器别名 / IMP-052 Python 启动推断 / IMP-053 多工作区复用提示 / BUG-454~458 修复** / **V0.7.3 IMP-055 路径别名兼容性门禁（撤销容器豁免、收敛 /assets 回退、access review API 对照、导入期守卫）/ BUG-467~469 修复** / **V0.7.7 IMP-058 五层流水线（Gate-A 静态预检 CHK-V02~V06 / Gate-B 多候选与能力契约 / Gate-C 实证校验状态机 VERIFYING→RUNNING·DEGRADED·FAILED / 证据驱动探针 / 能力守恒 / 降级策略 / CHK-192~193 BUG-475~485 修复）** / **V0.7.8 BUG-491 更新实例时保留已有 DATABASE_URL / BUG-492 Gate-C SQLite 回退扫描 / BUG-494 Docker 集成测试修复** / **V0.7.9 BUG-495～507 子目录 frontend/server 识别、Poetry 依赖、预检 REJECTED 置 pending、健康探针仅 GET/HEAD 且 2xx/3xx 通过、sourceSubdir 限制在 current 内** / **V0.7.10 BUG-508～522 网关切换加锁与完整回滚、损坏 manifest fail-closed、Caddy 失败恢复 builtin、迁移路径边界与 verify、回滚仅释本轮端口、status 以 registry 为准** / **V0.7.11 BUG-523～527 CHK-215 复审修复：builtin 恢复直接 `_start_builtin`、切换异常阶段不再误标 switch_lock、start 成功清 stale lastError、预检识别无空格 `-rfile`、registry 路径改写 substr 精确匹配** / **V0.8.0 IMP-059 update 三态 reconcile（enabled 未运行自动拉起 + `--no-reconcile`）/ IMP-060 doctor `service_runtime_state`·`restart_resilience` / IMP-061 自启缺省安全（with_caddy·linger 三态、init/setup 引导、运行模式标注）/ IMP-063 一键 GitHub 更新通道（fetch→固定 OID ff-only→pip→新解释器接力；`--check`/`--dry-run`/`--no-pull`/`--remote`/`--ref`；repo+workspace 双锁；skip-pip 门控；恢复链）** / **IMP-065 GitHub 源一键导入与更新（--from-git / --ref / --subdir；浅克隆 staging + ls-remote 无变更探测；管理页「从 GitHub 导入」不限 loopback；doctor git 检查 WARN）**；skills 数为 **20**） / **V0.8.1 stdlib-http 弱信号与识别优先级修复（BUG-533～544）** / **V0.8.2 自启幂等与 update 等待就绪（issues #2–#5，BUG-549～552）** / **V0.8.4 全量代码审阅收口（CHK-245，BUG-574～579）** / **V0.8.5 buildHooks/preStart 声明式构建钩子、rebuild 源码陈旧检测 + `--sync` + doctor `source_freshness`、.dockerignore 根级收窄（BUG-581）、别名注入 X-Real-IP（issues #6–#9）** / **V0.8.9 issue #18 pip 三源 `||` 切源与超时重试（`pipFallbacks`/`pipRetries`/`pipTimeout`）、issue #21 已验证别名保留 + 统一入口 404 兜底 + Gate-C 快照含别名、issue #22 非 root 模板 USER 前 `chown`、issue #23 钩子重建透传、IMP-064 服务意图去污染（`lastStartError`/熔断/内部停止原语/联动查意图）、IMP-062 doctor `version_freshness`（复用 `update --check` + 24h 缓存）、IMP-056 C.01–C.03（list/status 兼容性提示、scan 刷新 findings、alias 拒绝附线索）**。
- [ ] V0.8.10：issue #26 的 Gateway 启停/自愈路径统一使用可重入启动锁；`lwa update` 收尾检查单 Caddy master；Caddy stderr 写入 `logs/caddy-runtime.log`。
- [ ] V0.8.11：Fedora 43/44 纳入正式平台矩阵（门禁 / `install-*-linux.sh` dnf 路径 / 文档口径）；`lwa update` 对 venv 缺失 pip 自愈——快进前预检零变更阻断 + `ensurepip` 优先恢复链 + dry-run 风险标注（issue #27）；安全加固批次：monorepo 包名 `shlex.quote`、端口分配 bind 探测、`extraVolumes` 结构化校验与 YAML 引号渲染、`cap_add: ALL` 与下载-解释执行链审计及「审计非沙箱」文档对齐。
- [ ] V0.8.12：issue #28 源类型原地切换（`--from-dir/--from-git --update` 换源不换实例，保留 id/端口/别名/data/，内容一致仍完成切换）；issue #29 Docker 晚就绪快速收敛（daemon/manager 短退避 10s→20s→40s 封顶 60s、`setup --full --resume` 触发即时重探并等 daemon 收敛、ready 清 action 残留；BUG-625~628：退避指数封顶防溢出、先算后等消除滞后一轮、IPv6 重探 URL 加方括号）；BUG-613/614：launchd 重启不再连杀 caddy master（进程组隔离）、coordinated_start 对在跑单元改走 restart 不再假报已拉起。
- [ ] V0.8.13：DEV-132 实例级 `buildEnv` 构建环境变量（manifest 顶层字段，安装/构建命令以 `{**os.environ, **buildEnv}` 注入、重扫保留清单透传，根治烤入 `VITE_BASE` 被 `scan`/`import --update` 静默抹掉导致别名白屏；别名守卫拒绝 Vite 实例时错误文案附 LWA 侧最短解法）；issue #31 冗余批量删除护栏（携带路径别名 / buildEnv 的目标默认跳过并报 `skipped`，CLI `--allow-config-loss` / API `allowConfigLoss` 显式覆盖；管理页批量删除改确认弹窗逐项列处置 + 工具栏琥珀竖条释义，BUG-633/634）；DEV-133 管理页技术栈列 3 列网格换行；ADJ-048 管理页访问地址两行排版（端口/本机第一行、路径别名第二行，列宽取较宽者）+ 名称列上限 220→440px；BUG-636 全新导入 `--dry-run` 在打开工作区前拒绝（退出码 2，提示改用 `--update`，`--update` 保持无损预览）；BUG-637 `buildEnv` 校验加固（models 拒绝非法变量名与 CR/LF/NUL 值，保存前校验失败不改 manifest；守卫提示改为 `lwa configure` 命令并注明 vite.config 须读取 `process.env.VITE_BASE`、`--base` 仅限 build 脚本直调 Vite 且重扫不保留）；DEV-134 `lwa configure` 命令 + `PATCH /api/instances/{id}/settings`（buildEnv 查看/整组替换/清空、`--follow-alias-base` 构建期按当前别名推导 `VITE_BASE`（无别名为 `/`，优先于手动值，需手动 rebuild）、`--acknowledge-redundancy` 有意保留并退出冗余候选（可撤销）；容器运行时拒绝）；BUG-638 冗余清理加固（运行/期望运行/过渡态始终跳过且 `allowConfigLoss`/`--force` 不可覆盖、实例锁内复查候选与独立配置、管理页弹窗补别名/更新时间/保留列 + 「有意保留」按钮 + 允许损失配置复选框 + 零可删目标禁用确认）；BUG-639 冗余清理锁内复查复用预览分组 `keeperId` 仅复核目标与保留者（保留者丢失/指纹漂移退回全库复查，消除逐目标全库 ZIP 指纹重算的二次复杂度）；BUG-640 别名跟随开启时守卫提示改为先 `--no-follow-alias-base` 的四步迁移指引（faq / README DEV-132 同步）。
- [ ] V0.8.14：issue #32 manager 两把锁内核化（单实例/启动锁改 flock：死 PID、空/损坏锁、复用 PID 不拒启且绝不向无关进程发信号、锁文件永不删除、存活持有者不按年龄抢占；锁冲突按健康分类退出码——健康重复实例 exit 0，否则非零并带持有 PID/锁路径/日志位置；BUG-641/642）；旧版交接协议（锁记录加 `flock` 标记行、读 fd 前先核验 inode、旧版 manager 持旧式锁改为拒入并交协调层停止、启动锁对活跃旧版有限等待后拒入自愈；锁层不终止任何进程；BUG-645/646）；恢复指引保留自启（更新失败优先 `lwa update` 协调重启，off/on 注明停用自启并按服务恢复；BUG-643）与 `lwa autostart enable|disable --service` 按服务限定（保留其他服务原状态；BUG-647）；冗余清理配置保护补 buildHooks/preStart（预览与锁内复核同源；BUG-644）；update waitReady 按实际重启纳入 manager 就绪等待并区分本工作区；BUG-650/651：旧版交接等待以「锁路径被旧版 finally unlink」为临界区结束标志（旧 CLI 释放锁后仍存活，不再误报占用到超时）；两把锁遇遗留空记录（旧版「已建未写 PID」创建窗口）限时观察、绝不提前写入新协议记录（旧版恢复后会覆写记录并 unlink 新版持锁路径），新建文件立即写身份（旧版 EEXIST 读到存活 PID 自行让路），超时仍空按 inode 年龄 ≥60s（与旧版陈旧阈值同口径）判定崩溃残留后认领、未达阈值拒入自愈。
- [ ] V0.8.15：Agent 协作基座（AGC M0+M1）——发现入口 `/llms.txt` / `/agent-info.json` / `/agent-guide`（W03）；严格契约、13 工具元数据与共源 schema（W04，OpenAPI/MCP 同源）；本机 owner 认证、回环门禁与 `allowedSourceRoots` 源根校验默认拒绝（W06）；registry schema v2→v3（`workspace_meta`/`agent_plans`/`agent_operations` 幂等 DAO，W05）；`config` 新增 `AgentConfig`、`to_yaml` 改 mode=json 修 Path 序列化（BUG-653）；IPv6 回环 Host 头误拒（BUG-657）、空 result 降 NULL（BUG-658）、schema 共源缺跨字段规则（BUG-656）；新增 `docs/agent-guide.md`（W02）。注意：该版本发布时未同步 pyproject/fallback/CLI docstring/skills 版本号与 README 特性条目，已随 V0.8.16 补齐。
- [ ] V0.8.16：issue #33 doctor `service_version_drift`（比对三服务状态文件 `bind_version` vs 当前代码，WARN 指引对齐）+ `lwa services restart` 协调重启（保留自启、不拉码不装 pip，BUG-659；消费 updater 返回值区分已重启/跳过/熔断，熔断非零退出 BUG-663）；issue #34/#35 构建加固（DEV-139）——`aptFallbacks`/`aptRetries`/`aptTimeout` 与 pip 同构、manifest `systemDeps` 渲染于 `COPY current/` 前并带 BuildKit apt cache mount、官方源快照一次写入不可变（`/tmp/lwa-official-*`，每层各候选从原始源独立切换，BUG-661）、buildHooks 原样渲染不自动改写（系统包迁 `systemDeps`）、pip 仅精确 `pip install .` 拆依赖层（extras/附加参数/复合命令整包 COPY 保语义，BUG-660）、Node/Alpine 容器拒绝 `systemDeps`（BUG-664）；构建失败分类（build_diagnostics：网络/内存/磁盘，`Killed` 标不确定，BUG-666）写入 `lastError`、探针失败附容器日志尾部与 inspect 证据（`RestartCount` 取容器顶层、未知不伪造，BUG-665）；`desiredAlias` 活验证回滚后保留并在 start/rebuild 成功后自动补登记（显式 `alias clear` 清空期望态且自动恢复先重读磁盘意图，BUG-662）；daemon 构建熔断（reconcile_circuit：连续 3 次小时级退避、5 次转人工，`lwa start`/`rebuild` 清熔断，`lwa status`/管理页「构建熔断」字段）；`lwa configure --system-deps/--build-hook`；文档同步（DOC-180/181：faq/operations-playbook/known-limitations/README）。
- [ ] V0.8.17：issues #36–#39 收口 + 两轮复审修复（BUG-667~687）——issue #37 活验证读别名实际服务 HTML、空探针集不盖 `aliasLiveVerified`（BUG-675/678）；issue #38 守卫覆盖 restart/recover 与双向网关切换、切 builtin 记 `skipped`+本次时间（BUG-676/679/686）；issue #39 容器 `buildEnv`/别名跟随放行——Dockerfile `ARG`/`ENV` + Compose `build.args` 字面值传递、管理键（`HOST_PORT` 等）拒绝、`$` 不二次展开（BUG-680/681），Python 容器前端自动构建加 **npm 契约门禁**（build 脚本 + `package-lock.json`/`npm-shrinkwrap.json`；pnpm/yarn/缺件跳过留注释交 `buildHooks`，BUG-683/684），别名跟随提示四步迁移顺序（BUG-685）；issue #36 Docker Hub token 拉取不误判 apt、失败区域保留失败步骤同步骤前文（`ERROR:` 前 `Killed` 不丢，BUG-677/682/687）；发布后加固（BUG-667~673：`bind_version` 回写竞态、熔断持久化入实例锁、lastError 截断一致 1800、Node tarball URL 随 apt 源逐次切换、doctor 漂移不假绿且不硬编码 127.0.0.1、restart/recover 清熔断及一组 P3）；Agent M1 查询/计划服务层（AGC W07/W08 `agent/service.py`，只读不执行构建）与 registry schema v4 revision CAS（W09，BUG-674 修 git subdir 越界校验）——HTTP/MCP 入口未实施；文档同步（DOC-182/183 + BUG-684/685 faq 口径）。发布门禁：全量 pytest 2914 passed/12 Docker skipped、ruff/mypy 全绿（BUG-202）。
- [ ] V0.8.18：Agent M1 本机协作通道落地（AGC W10–W14）——`GET/POST /api/agent/v1/*` 严格新路由随 manager 上线：能力快照 / 实例分页与详情 / 访问地址 / 脱敏日志只读，`POST /plans` → `POST /deployments`（源码快照+digest，TTL 30 分钟）、异步操作（202 + operation，轮询/取消）与异步生命周期 start/stop/restart/rebuild；写操作强制幂等键（同键同体返回同一 operationId，同键异体 409）、`expectedRevision` 乐观锁、队列超限 429 busy、契约化错误体（ERROR_SPECS）；鉴权比管理页更严——仅回环 + `Authorization: Bearer`/`X-LWA-Token`（无 `?token=`）。新增 `lwa mcp --workspace <path>` stdio MCP 适配器（工具与端点一一对应；pyproject 新增 `[mcp]` extra：`mcp>=2.1,<3`）、`lwa agent connection-info` 接入引导（apiBase/workspaceId/契约版本/配置自检，不输出 token）、AgentWorker 持久操作 worker（单线程租约轮询，崩溃恢复置 interrupted，同幂等键重提安全）、`agent/guides/` 三份 Agent 指南（quickstart/deploy/troubleshooting，`{{PRODUCT_VERSION}}` 动态替换）。文档同步：版本落点 8 处统一 0.8.18（pyproject/version_info/cli docstring/test_version_info/两份 lwa-update-runtime SKILL/agent-guide 状态行/guides 版本行）、README 双语新增 V0.8.18 特性条目与命令表 Agent 小节、manager-page 补 `/api/agent/v1/*` 行与鉴权说明、agent-guide §9 改写为已落地。评审修复批次（BUG-688~695，见 design/5-reviews/2026-09-17-uncommitted-m1-w10-w15-review.md）：queued 取消竞态不再假报 cancelled（CAS 返回值必看，失败重读实际状态）；幂等重放先于队列上限判定（满队同键重试返回原 operationId 而非 busy）；apply 后源身份写回 manifest（folder 带 sourceSyncHash、git 带 §17.2.1 全字段），git 源实例 update 计划期拒绝 + worker 兜底；recover_stale 以进程内 in-flight 集合替代身份一刀切跳过（本进程悬挂过期租约行也回收）；cancelling 期间租约可续约；connection-info docstring 凭据口径修正；localhost 回环判定与 MCP bridge 统一（agent.auth.LOOPBACK_HOSTS 共源）；get_logs 新增 category 定向读取（build/run/gateway/import/scan），每个 bug 附回归测试。发布门禁：全量 pytest 2975 passed/12 Docker skipped、ruff 全绿、mypy 102 源文件无问题（注意：agent 验收测试在 builtin 网关下会为部署实例拉起 `http.server` 子进程且 fixture 不回收，多次跑测会累积占用 23000-23050 测试端口池导致后续跑测 PORT_ERROR，跑前需 `pkill -f 'http.server.*pytest-of-'` 清理，测试回收问题待修）。
- [ ] V0.9.0：HTTPS 首版交付（CHK-352 提前立项，DEV-155/W01–W12；V0.8.19-Build4202 提交后追加 BUG-726/728 复审修复与 issue #43 认领修复 BUG-729）——`gatewayTls: internal`（仅 Caddy）：别名入口 `https://<LAN-IP>:8443/`、管理面独立 HTTPS origin `https://<LAN-IP>:9443/`（反代回环 manager，非同源 /manager/）；Caddyfile global 块 `skip_install_trust`+`auto_https disable_redirects`（BUG-710：禁 sudo 装根证书与明文 :80）；internal CA 归属 workspace `run/caddy-data`（XDG 注入，0700）；`lwa ca export` 导出根证书+SHA-256 指纹+安装指引；TLS 入口验证语义=任何完整 TLS 握手的 HTTP 响应即健康、证书错立即失败不降级明文（BUG-711）；明文收敛（manager 强制回环 / instanceBindHost 三处绑定含 Docker host-ip 与 IPv6 / 明文 8080 默认关闭）；URL 与探活全链路 https+证书验证（lanUrl 保持直连明文语义 BUG-713；status 读时合成 BUG-714）；反代鉴权 XFF 链校验防 LAN 伪造 Host（W09）；doctor `gateway_tls` 检查（W10）。同批收口：issues #40–#42（BUG-700 sourceSubdir 前端构建 / BUG-701·702 doctor 版本判定质量 / BUG-703 mcp.enabled 探测+command 模板+quickstart/llms.txt）；M1 审查整改 BUG-704/705/706~709（update_zip 锁内 expectedRevision、能力缺口 apply/worker 复验、幂等重放先于易变校验、跨字段校验 JSON 安全、契约 schema 完整发布）与 BUG-710~716（CHK-353/354）；管理页排版优化（ADJ-049：表格线框/字体/名称列防挤压）。⚠️ 真机验收：两台设备证书信任/明文拒绝/无旁路清单见 docs/https.md §7；sudo 弹窗消失与 :80 不监听需实机复核（BUG-710 行为修复）。复审修复批次：BUG-726（TLS 下 JS bundle 抓取明文打 https 端口致 IMP-055 退化）、BUG-728（已在线分支 TLS 失败不对称收尾+25s 阻塞）；issue #43 / BUG-729（pidfile 被 CHK-280 共享删除后 off/on 卡死熔断终局——四重安全闸自动认领重建 pidfile，start/stop/doctor 三处接入，回归 6 用例）；issue #40 收尾 / BUG-730（lwa mcp fatal 双通道诊断 lwaMcpFatal JSON 行、doctor mcp_dependency 检查、agent-guide 前置 [mcp] extra、instanceId 契约 description 双通道携带）；BUG-727（uv.lock 重跑对齐 0.9.0，入库后版本提升须同步 uv lock）。Build 号建议 4202→4300 延续序列。
- [ ] 工作区无未提交的调试代码、`print`、`TODO` 残留（`grep -rn "TODO\|print(" src/`）。

## 测试

- [ ] `python3 -m pytest` 全绿，无 unexpected skip。
- [ ] 静态门禁：`python3 -m ruff check src tests` 与 `python3 -m mypy src/local_webpage_access` 均为 0（配置见 `pyproject.toml`，ADJ-033）。
- [ ] 端到端验收 `tests/test_e2e_acceptance.py` 全部通过。
- [ ] 在具备 Docker 的 Linux 主机上执行 `LWA_RUN_DOCKER_TESTS=1 python3 -m pytest tests/test_docker_integration.py`。
- [ ] 按 [acceptance-checklist.md](acceptance-checklist.md) 完成手工验收（尤其 WBS-29.09/11/12 容器构建启动与数据持久化；以及 033.13 Full/systemd、035.06 删除浏览器、036.08 平台矩阵）。
- [ ] 验收记录与问题清单已填写（acceptance-checklist.md 的「验收记录」「问题清单」两节）。
- [ ] 036.09：确认 daemon/manager 在 win32 上拒绝 DETACHED 启动（单元测试覆盖）；文档无 Windows 原生推销。
- [ ] IMP-042.b 跨盘/跨机迁移仍标延期，勿在发布说明中宣称已支持。

## 文档

- [ ] [README.md](../README.md) 已更新（含管理页、daemon、doctor、capabilities、Full Profile、skills、浏览量、冗余、运维手册、`setup|init --full --resume`、正式支持平台索引、`gateway switch` / `cancel-build` / `doctor --access` / LAN stale / `workspace relocate` / IMP-043 显示名 / V0.6.12 工作区一致性与挂载漂移防护 / V0.6.13 pageviews 轮转与显示名回填加固 / **IMP-046 token 轮换** / **IMP-047 `--from-dir`** / **IMP-051 选择文件夹** / **IMP-058 五层流水线 Gate-A/B/C** / **V0.8.0 IMP-059 三态 reconcile·IMP-060 doctor 新检查·IMP-061 缺省安全·IMP-063 一键更新通道 / **IMP-065 `--from-git` GitHub 源导入与更新**） / **V0.8.5 rebuild `--sync` 与源码陈旧检测、buildHooks/preStart、X-Real-IP**。
- [ ] [faq.md](faq.md) / [operations-playbook.md](operations-playbook.md) / [known-limitations.md](known-limitations.md) / [manager-page.md](manager-page.md) / [autostart.md](autostart.md) / [workspace-rename.md](workspace-rename.md) 与 IMP-033/034/035/036/037/038/039/040/041/042/043/**046/047/051**/**058**/**059/060/061/063** 行为一致。
- [ ] [docs/manager-page.md](manager-page.md) API 端点表与实际路由一致（含 pageviews / redundant / remove / path-alias IMP-022；`POST .../cancel-build`；`POST /api/gateway/switch`；`POST /api/access/refresh`；删除模态焦点管理；前端取消构建与 LAN stale 横幅；IMP-043 显示名/`nameSource`/产物目录 title；**IMP-046 token 轮换**；**IMP-047 import-from-dir / update-from-dir**；**IMP-051 pick-directory**）。
- [ ] [docs/operations-playbook.md](operations-playbook.md) 与网关选型 / `gateway switch` / 访问复核 / `cancel-build` / 宿主机装配档位 / 冗余 / 容器别名 / Caddy 排障一致。
- [ ] [docs/faq.md](faq.md) 覆盖导入/容器/管理页/端口/磁盘各类排障（含 slug 冲突与 `--update`、内置 Docker/Caddy 安装，不再写自动 `-2/-3`；`doctor --json` 未初始化亦可输出 platformSupport；取消构建；网关切换 `accessOk`/`fullyOk`；删除对账）。
- [ ] [docs/security-boundary.md](security-boundary.md) 审计项与 `security.py` 实现一致。
- [ ] [docs/known-limitations.md](known-limitations.md) 明确 V1 不支持范围（含 `.env.local`、冗余批量例外、Ubuntu LTS / Debian Stable / Fedora 滚动窗口矩阵）。
- [ ] [docs/testing.md](testing.md) 测试分层与跳过条件准确（含 pageviews / build_queue / zip_processor）。
- [ ] [docs/acceptance-checklist.md](acceptance-checklist.md) 18 个子任务有结论。

## 安装与冒烟

- [ ] 干净虚拟环境中 `pip install -e .` 成功，`lwa version` 输出版本号。
- [ ] `pip install -e ".[dev]"` 成功，`python3 -m pytest` 可运行。
- [ ] 全新目录 `lwa init` → `lwa import <样例 zip>` → `lwa start` → `lwa status` 全链路通过。
- [ ] `lwa manager on`（或前台 `lwa manager start`）能打开管理页；本机读 API 免 token、写需同源或 token，局域网访问需 token 登录。
- [ ] `lwa doctor` 在干净环境全部 ok/warn，无 fail。
- [ ] `lwa daemon on` 能自动导入 `inbox/` 中的 zip。

## 安全核对

- [ ] 生成的 compose.yaml 通过 `audit_compose` 无 critical（`test_security.py::test_generated_compose_passes_audit`）。
- [ ] 生成的 Dockerfile 通过 `audit_dockerfile` 无 critical（`test_security.py::test_generated_dockerfile_passes_audit`）；含 `ADD https://` 或 downloader 直接解释执行链时拒绝写出（`test_generate_dockerfile_rejects_*`）。
- [ ] 管理 token 在绑定 `0.0.0.0` 时必须存在（`validate_manager_binding`）。
- [ ] zip slip 防护在导入层与审计层双重生效。
- [ ] 容器以非 root 用户运行（Dockerfile 模板确认）。

## 发布动作

- [ ] 在 `main` 之外的发布分支上操作（或按团队流程）。
- [ ] 更新 CHANGELOG（如有）。
- [ ] 打 tag：`git tag -a v1.0.0 -m "V1 release"`。
- [ ] 推送 tag 与分支。
- [ ] **源码发布 zip（BUG-202）**：在仓库根执行 `bash scripts/pack-release-zip.sh`，产物必须含 `pyproject.toml` + `src/`（可选 `docs/`）。禁止只打 `src/`+`docs/` 的残缺包（会导致 `pip install -e` 丢失 `lwa` 入口）。
- [ ]（可选）构建 wheel：`python3 -m build`，校验产物。
- [ ]（可选）发布到 PyPI / 内部源。
- [ ] 在仓库 Release Notes 中链接到 `docs/acceptance-checklist.md` 的验收记录。

## 回滚预案

- [ ] 确认 `git revert` 可回到上一个稳定 commit。
- [ ] 确认工作区数据（`apps/<id>/data/`、`registry/local-web.db`）在回滚后仍可被旧版本读取（schema 未变）。
- [ ] 记录发布负责人与联系方式，便于线上问题响应。
