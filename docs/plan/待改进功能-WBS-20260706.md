# 待改进功能 WBS（20260706）

承接 [`待改进功能点记录-20260706.md`](待改进功能点记录-20260706.md)，把待规划的 6 项（IMP-001 / 005 / 006 / 007 / 008 / 009）拆解为可直接实现的子任务。编号沿用 `IMP-NNN.MM`；每个 IMP 给出**触点文件、子任务、验收口径、测试点**。

实现顺序按依赖与风险：**001（剥离，是 009 的前置）→ 007（最小、纯展示）→ 005（限流）→ 006（路径别名）→ 008（update）→ 009（实例更新，依赖 001 与 006 的部分能力）**。每完成一项：补单测 → 跑目标 pytest → 写入 `task-list.md`（DEV/OPS 前缀，动作枚举按规则归类）→ 全量回归留到末轮。

---

## IMP-001 zip 导入前自动剥离冗余包与缓存

| 子任务 | 触点 | 说明 |
| --- | --- | --- |
| 001.01 | `security.py` | 新增 `sanitize_zip_members(names, modes)`：按路径前缀把成员分成 `(keep, stripped, summary)`；剥离规则见计划文档（`node_modules/`、`__pycache__/`、`.venv/`、`venv/`、`env/`、`.git/`、`.DS_Store`、`__MACOSX/`、各类 cache）；保留 lockfile / 源码 / `dist/` / `build/` / `public/` / 配置。 |
| 001.02 | `importer.py` | `_safe_extract` 改为：先 `sanitize_zip_members` 分类，**剥离成员不落盘、不参与后续 zip_slip 审计**；对 keep 成员继续 `audit_zip_members(modes=)` + 路径穿越校验 + 解压后 symlink 深度防御。剥离摘要经 `ImportResult.sanitized` 暴露。 |
| 001.03 | `cli.py` | `lwa import` 输出剥离摘要（剥离目录、文件数、是否含 symlink、原始 vs 净化体积估算）。 |
| 001.04 | `tests/test_importer.py`、`tests/test_security.py` | 含 `node_modules/.bin` symlink 的 zip 一键 import 通过；保留 `package-lock.json`；源码目录恶意 symlink（`src/evil→/etc/passwd`）仍 `zip_symlink` 拒绝；zip slip / 盘符 / 绝对路径回归不退化。 |

**验收**：声纹 V3 含 `node_modules` zip 直接 import+start；剥离可审计；安全回归全绿。

---

## IMP-005 Caddy 静态站点频率限制（内网防护）

| 子任务 | 触点 | 说明 |
| --- | --- | --- |
| 005.01 | `config.py` | 新增 `StaticRateLimit{enabled:bool, rps:int, burst:int}`（默认 `enabled=false`、`rps=3`、`burst=6`）；字段范围校验；写入 `CONFIG_EXAMPLE`。 |
| 005.02 | `static_gateway.py` | 新增 `supports_rate_limit()`：`caddy list-modules` 探测是否含 `http.handlers.rate_limit`（缓存结果）。Caddy 站点配置模板在 `enabled && supports` 时注入 `rate_limit` directive；不支持时 `WARN` 但站点仍可访问。 |
| 005.03 | `static_gateway.py` | `_assemble_main_config` 注入全局需要的 directive（如需）；reload 失败走既有回滚（BUG-011）。builtin 模式文档化「不支持限流」。 |
| 005.04 | `tests/test_static_gateway.py` | 配置生成正确（enabled/未 enabled / 能力不可用三种）；能力探测 mock；不引入真实 Caddy 依赖。 |

**验收**：能力可用时超阈值 429；不可用时 WARN 且站点正常；reload 失败可回滚。

---

## IMP-006 路径别名路由（显式启用）

| 子任务 | 触点 | 说明 |
| --- | --- | --- |
| 006.01 | `cli.py`、`importer.py` | `lwa import ... --path-alias <slug>` 可选；未传时**默认行为不变**（仅 hostPort 访问）。alias 写入 manifest `network.routeMode=port|name` + `network.routeHost`。 |
| 006.02 | `paths.py`/`security.py` | 新增 `validate_path_alias(slug)`：`^[a-z0-9]+(-[a-z0-9]+)*$`；全局唯一（registry 扫描）；保留字（`api`、`static-gateway`、`inbox`、`apps`、`registry`、`run`、`manager`、`logs`、`skills`、`templates`、`health`）拒绝。 |
| 006.03 | `static_gateway.py` | Caddy 模式主配置注入 `/<alias>/* → reverse_proxy localhost:<port>` 的 strip-prefix 路由；`/<alias>` 301 到 `/<alias>/`。builtin 模式不支持 alias（CLI 显式提示，仍只给端口）。 |
| 006.04 | `lifecycle.py` | `remove_instance` 清理别名路由；`restart_instance`/`stop_instance` 保留 alias 登记、按需禁用 upstream（V1：stop 即整站 disable，alias 随之不可达，简化处理）。 |
| 006.05 | `cli.py` | `lwa start` 输出端口 URL 与（有别名时）路径 URL。 |
| 006.06 | `tests/` | 不传别名默认行为不变；传别名端口 + 路径并存；冲突/保留字拒绝；remove 后路由清理；Caddy 生成文本断言。 |
| 006.07 | `manager_static/{app.js,style.css}` | **管理页操作区**增加「**路径别名**」按钮（与启动/停止/重启并列）。仅 **静态实例**（`runtime=shared-static`）且非 `pending/building/queued` 时可点；容器实例置灰并 tooltip「路径别名仅支持静态站点」。点击弹出对话框：展示当前别名（无则空）、输入新 slug、确认/取消；错误时在对话框内展示 API 返回的校验信息。 |
| 006.08 | `manager_api.py`、`hosting.py`/`lifecycle.py` | 新增 `PATCH /api/instances/{id}/path-alias`（或 `POST .../path-alias`），body `{ "alias": "voiceprint-demo" }` 或 `{ "alias": null }` 清除别名。服务端复用 `validate_path_alias`（格式、保留字、全局唯一——**排除当前实例自身**）；写入 manifest `static.routeMode`/`routeHost` 与 `network.routeMode`/`routeHost`/`routeUrl`；同步 registry `static_sites.route_host`；若实例 **running** 则 regenerate 别名片段 + `reload_all`（Caddy）；清除别名时删除 `aliases/<id>.conf` 并 reload。builtin 模式保存登记但提示「别名入口未启用，仅端口可达」。 |
| 006.09 | `cli.py`（可选） | 新增 `lwa alias set <id> <slug>` / `lwa alias clear <id>`，与管理页/API 共用同一套 Python 函数，便于脚本与 Skill 调用。 |
| 006.10 | `tests/test_manager_api.py`、`tests/test_paths.py` | API：设置/修改/清除别名；保留字与 duplicate 返回 400；改别名后旧路径 404、新路径可访问（mock gateway）；running 实例改别名触发 reload；非静态实例 400；管理页 E2E 可选手测清单写入 `docs/acceptance-checklist.md`。 |

**SPA 限制**：写死 `/assets/...` 绝对路径的 SPA 在子路径下资源 404；V1 在 `--path-alias` 帮助文本与管理页对话框内明确提示，不强行改写 base。文档同步 `known-limitations.md`、`docs/manager-page.md`。

**006.07~006.10 验收（管理页改别名）**：

- 列表操作区可见「路径别名」按钮；静态 running/stopped 可改，容器/进行中态不可用。
- 修改别名后列表「访问地址」列与详情 `routeUrl` 同步更新；`lwa status` 与 API 口径一致。
- 校验规则与 CLI `--path-alias` **完全一致**（仅多一个管理页入口，不放宽规则）。
- 清除别名（传 `null` 或空串）后恢复纯端口访问，旧 `/<alias>/` 404。
- remove 实例仍清理别名片段（回归 006.04 不退化）。

---

## IMP-007 管理页展示端口映射关系

| 子任务 | 触点 | 说明 |
| --- | --- | --- |
| 007.01 | `status.py`、`manager_api.py` | `InstanceStatus` 增加 `internalPort`、`portMappingLabel`（后端统一格式化，列表与详情一致）；`/api/instances`、`/api/instances/{id}` 返回。 |
| 007.02 | `manager_static/{index.html,app.js,style.css}` | 端口列主显示 hostPort，副信息 `internalPort→hostPort`（容器/有内部端口时）；静态不显示误导性映射。详情抽屉同步。 |
| 007.03 | `cli.py` | `lwa status`/`lwa list` 已有端口列；补一行 `internalPort→hostPort`（仅当有内部端口且不同时）。 |
| 007.04 | `tests/` | 容器实例显示映射；静态实例不显示 `80→hostPort`；缺失 internalPort 不报错。 |

**数据口径**：`hostPort`=宿主访问端口；`internalPort`=容器/应用内部监听端口；缺失时 `portMappingLabel=None`。

---

## IMP-008 `lwa update` 工作区热重载

| 子任务 | 触点 | 说明 |
| --- | --- | --- |
| 008.01 | `updater.py`（新建） | 识别 workspace（`local-web.yml`）与 repo（editable 安装路径 / 当前 git 根 / `--repo`）；预检：版本、manager/daemon 运行态、running 实例、待同步 skills/templates 清单。 |
| 008.02 | `updater.py` | 执行步骤：`pip install -e .` → sync skills（复用 `init_workspace._copy_default_skills`）→ 可选 sync templates（默认关）→ 配置缺省字段补齐（不破坏性）→ 重启 manager/daemon（仅原本启用时）→ `lwa doctor`。每步独立失败不中断后续，最终退出码反映失败。 |
| 008.03 | `cli.py` | `lwa update [--dry-run] [--skip-pip] [--sync-templates] [--no-doctor] [--restart-instances] [--json]`。`--dry-run` 只展示计划；`--json` 输出草案结构。 |
| 008.04 | `manager_service.py`、`daemon.py` | 暴露 idempotent 的 restart helper（off→on）供 updater 复用；stop 失败时返回错误摘要而非 exit。 |
| 008.05 | `tests/test_updater.py`（新建） | mock `subprocess`：仅 sync skills、manager 重启失败提示、daemon 原本 stopped 不被启动、`--dry-run` 不产生变更、`--restart-instances` 跳过 building/queued/pending。 |

**安全边界**：pip 成功但 manager 重启失败不回滚 Python 包；提示查 `run/manager.json`、`logs/`。

---

## IMP-009 实例 zip 包更新（再导入识别与原地升级）

| 子任务 | 触点 | 说明 |
| --- | --- | --- |
| 009.01 | `importer.py` | 新增 `Importer.update_zip(zip_path, instance_id, *, restart=True, keep_data=True, yes=False, dry_run=False)`：校验实例存在 → 计算 hash 与 `sourceZipHash` 比（相同跳过 + 提示）→ sanitize（复用 001）→ 备份原 zip `source/original.zip.bak` → 清空 `current/`（保留 `data/`）→ 解压新内容 → 重扫 → **kind/runtime 变化拒绝**（首版，除非 `--force-kind-change`）→ 更新 manifest（保留 id/hostPort/data/desiredState，刷 `sourceZipHash`/`updatedAt`）→ registry 同步。失败回滚 `current/` + manifest。 |
| 009.02 | `cli.py` | `lwa import <zip> --update <id>`；slug 冲突不再 silent 建 `-2`：提示「已存在，加 `--update` 或换 `--name`」。`--yes` 非交互；`--dry-run` 展示 hash diff。 |
| 009.03 | `lifecycle.py` | 更新成功后若 `desiredState=running`：`stop`（或仅停网关）→ `rebuild`/`host_instance` → 恢复 running，**复用既有 hostPort**（不重新分配）。事件链：`update` 事件带旧 hash→新 hash。 |
| 009.04 | `manager_api.py`、`manager_static/` | `POST /api/instances/{id}/update`（body `{ "zipPath": "inbox/foo.zip" }`），复用 CLI 同一套 Python 逻辑；列表/详情可展示 `sourceZipHash` 前 8 位。 |
| 009.05 | `skills/lwa-import-zip/` | 文档化「同项目新版本 → `--update <id>`」；助手收到新 zip 先查 registry 是否同 slug，询问或带 `--update`。 |
| 009.06 | `tests/test_importer.py` | hash 相同跳过；kind 变化拒绝；失败回滚保留 `current/` 与 hostPort；`data/` 持久文件更新后仍在；不传 `--update` 且 slug 冲突报错而非建 `-2`。 |

**与 008 区分**：008 = 工具升级；009 = 业务 zip 升级。可组合：`lwa update` 后 `lwa import --update`。

---

## 末轮验证

- 全量 `pytest`（当前基线 625 passed/4 skipped）；新增模块测试全绿。
- `python -m compileall src`；`node --check manager_static/app.js`。
- `impeccable detect` 前端改动无 slop。
- 文档同步：`README.md`（新 CLI 选项）、`docs/manager-page.md`（新 API/字段、路径别名对话框与操作按钮）、`docs/known-limitations.md`（SPA 子路径、builtin 不支持 alias/限流）、`docs/runtime-workspace.md`（`lwa update`）。
- 版本号 bump（`version_info.py` + commit 主题）。
